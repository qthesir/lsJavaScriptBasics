// In the code below, identify the following items: Function arguments, function body
// function declaration, function invocation, function name, function parameters
// function return value, the names of all variables in this program

/*function declaration*/ function /*function name*/ multiplyNumbers(
  /*parameters*/ num1,
  num2,
  num3
) {
  //function body
  let result = num1 * num2 * num3;
  return result /*function return value*/;
}

let product = /*invocation*/ multiplyNumbers(/*arguments*/ 2, 3, 4);

// Names of all variables: result, product, num1, num2, num3
// The list of variables ALSO includes multiplyNumbers. The function is technically
// a variable, or technically an object.
