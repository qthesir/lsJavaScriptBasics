// What does the following code log to the console?

function scream(words) {
  return words + "!!!!";
}

scream("Yippeeee");

// This time, the code does return something other than undefined. However, it will
// still not log anything, because there is no console.log() statement.
